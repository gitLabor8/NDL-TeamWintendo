This folder is used by the console (one of our computers) to buffer the pictures received from the right Joy Con.
These temporary files are not tracked by Git, so this folder probably looks pretty empty for you.
