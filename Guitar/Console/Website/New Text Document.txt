// First Guitar key to button mapping
function guitarButtonRed (bit1) {
  var button = null;
  if(bit1 = 1)
    button = "redButton";
  }
  return button;
}

// Second Guitar key to button mapping
function guitarButtonRed (bit2) {
  var button = null;
  if(bit2 >= 1)
    button = "yellowButton";
  }
  return button;
}

// Third Guitar key to button mapping
function guitarButtonRed (bit3) {
  var button = null;
  if(bit3 >= 1)
    button = "greenButton";
  }
  return button;
}

// Last Guitar key to button mapping
function guitarButtonRed (bit4) {
  var button = null;
  if(bit4 >= 1)
    button = "blueButton";
  }
  return button;
}